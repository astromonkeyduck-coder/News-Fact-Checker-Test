diff --git a/STATUS.md b/STATUS.md
--- a/STATUS.md
+++ b/STATUS.md
@@ -1,46 +1,46 @@
-# Noteworthy Games — execution status
+# Noteworthy Games — saved checkpoint
 
-Active brief: `/Users/richarda/Downloads/Noteworthy_Games_Astra_Rebuild_Prompt.md`, explicitly authorized by the user September 13, 2026. Complete all first-release scope; do not stop at a sample. No production deployment, live communications, purchases or external commitments.
+Paused at the user's explicit request to reach a good stopping point on September 13, 2026. This checkpoint supersedes the earlier instruction to continue through the entire release in this turn. No production deployment was performed by this task.
 
-## Location and preservation
+## Open and resume
 
-Implementation: `/private/tmp/noteworthy-publication-release`, isolated copy of `/Users/richarda/breaking-news-game`. Original working checkout has existing unrelated changes; preserve them. Prior publication redesign and Inside the Story feature are complete and durably saved in original `publication-release-review/` (170 files; 209 passing tests; package applycheck and materialize/rebuild verified). The running preview is port4173 using `scripts/preview-publication.js`. New games remain modular in games/. Existing game HTML copied to games/legacy-fact-checker.html and legacy-geography.html with root base href; old scripts, saves, backend records are retained.
+Working preview: http://127.0.0.1:4173/games/
+Implementation: /private/tmp/noteworthy-publication-release
+Durable checkpoint: /Users/richarda/breaking-news-game/games-release-review/
+User brief: /Users/richarda/Downloads/Noteworthy_Games_Astra_Rebuild_Prompt.md
 
-## Creative decision
+The checkpoint contains the Games source, data, tests, screenshots, documentation, route aliases and build integration. Its README and manifest explain recovery without changing the current checkout. Browser progress remains in the versioned noteworthy-games-v1 local-storage record. Journal also supports export. Legacy keys and backend records are preserved.
 
-Three concepts considered: (1) Waterline Files: terrain/water and passive-tracer transport reveal causal connections and uncertain explanations; (2) Echo Bureau: sound propagation/echoes locate recordings and challenge timestamps; (3) Shadow Archive: sun/occlusion/image geometry tests image claims. Waterline chosen because a shared coupled model supports connection, timing, identifiability and intervention investigations across a coherent six-mission season. The fictional Merehaven estuary is an educational world, not a real emergency or agency system.
+## Implemented
 
-## Shared contracts
+Three concepts were developed: Waterline Files (coupled water/tracer investigation), Echo Bureau (sound localization and recording provenance), and Shadow Archive (sun/occlusion/image geometry). Waterline Files was chosen and implemented in fictional Merehaven. See docs/games-design.md.
 
-Plain JavaScript/UMD modules, no new framework. Local scene1200×800metres, x east/y north,24×16 cells50m. Time seconds; waterdepthm, tracerg/concentrationg/m³. Regional GIS is planar localmetres and explicitly separate from geographic atlas coordinates. Physics immutable inputs+deterministic clock, serializable state and budgets. Every analysis carriesinputs/units/timewindow/assumptions/output/version. Observed synthetic case records immutable; counterfactual results distinct.
+- Six integrated missions across three chapters, six Case Files and 24 Quick Checks (six of each verdict).
+- Coupled finite-volume water and tracer simulation, fixed synthetic field observations, controlled experiments, named runs, input sensitivity ranges and saved field briefings.
+- Measured distance, area, buffers, time-filtered selections, spatial joins, drainage paths, terrain profiles, layered maps, probe graphs and numeric tables.
+- Physics sandbox, ten-target Atlas Expedition, Classic 50, Hard 50, Typing 50 and nonvisual geographic reasoning, using frozen sourced population and map data.
+- Versioned saved progress, fixed scoring, journal, achievements, personal bests, missed-item review and spaced mastery rules.
+- Daily three-claim/five-country flow and optional timed Quick Check implemented; final complete browser playtests remain pending.
 
-## Work in progress
+## Actual validation at pause
 
-- Root: core shell, persistence/scoring/daily, evidence play, investigation UI/worker, integration, browser playtests, release package.
-- editorial_workflow agent: world, coupled physics, GIS, numerical/spatial tests and model notes.
-- visual_review agent: six mission/sixshortcase/24quickitem content; first guidedmission+case beforefullseason; content validation.
-- source_repairs agent: real population-derived Classic50dataset, geography geometry+UI+Expedition/Hard/Typing/reasoning, scoring/alias/mastery tests.
+The first integrated slice was completed and improved before expansion: the evolving archive-image Case File scored 40/40 then 60/60 after refresh, and the first mission scored 100/100 using real 3600-second runs, a 400-metre buffer, a controlled comparison and explicit limits.
 
-## Required sequence
+Fresh final checks passed: 116 Games tests; 126 publication tests using tests/publication/*.test.js and tests/publication-source.test.js; publication/Games build; syntax checks for app, shared UI and investigation controller. No live service writes are part of these commands. The earlier publication package retains its separate broader validation record.
 
-1. Baseline/architecture and conceptpass (done). Research primary references and publish sharedinterfaces (ongoing).
-2. Build one complete integratedinvestigation + oneCaseFile + ten-targetExpedition. Play and repair them BEFORE expanding six-missionseason.
-3. Finish6integratedmissions/3chapters,6casefiles,24quickitems,sandbox,Classic50/Hard/Typing,sharedUTCDispatch,journal/mastery/achievements/returningprogress,casualsame-seedfriendlinks.
-4. Complete meaningfulunit/integration/numericalspatialtests; actualbrowser completions ofeachmode; responsive375/390/768/1280/1440 andkeyboard/reducedmotion; independentreview.
-5. Packagecompleteimplementation, artifacts, inventories/licenses/reviewpending/modelnote/validation/maintenance. Specific externaldependencies truthful. No production publication.
+Every accepted conclusion across all six missions is covered by actual computed numerical fixtures. Independent browser play completed mission 2 at 100/100 and tested sandbox replay, probes, GIS filters/joins, keyboard map controls and a two-phase storm whose 16000 m³ rainfall ledger matches the analytical calculation. Notebook graph-location caption repair was independently retested successfully.
 
-## Baseline evidence
+Root completed the finale at 100/100 in the browser: compared permeability 48 vs 28 with the same lowered parcel, computed a real profile and rainfall runs at 52/65/78 mm/h, and filed a bounded trial conclusion. The saved map correctly used the selected 3600-second run even with the live model reset to zero. Its standalone briefing images loaded successfully. Atlas browser completion and responsive evidence are in docs/games-atlas.md and tests/games/atlas-browser-evidence.json.
 
-Existing stack is staticHTML/JS + Netlifyfunctions, existing webpack script. Baseline localFactCheckerwelcome Startclick didnotadvance; browserreported Leaderboard ReferenceError. OriginalGeography localmap displayedMapLoadingError (failedfetchworld-map.svg), HUDpushedmapfarbelowtop. These observations apply to isolatedpreview, not assertedproductiondefects. Baselinescreenshots docs/screenshots/games-before-*.png.
+## Resume here — remaining release gates
 
-Code scoring has base10difficulty×level andspeedbonus50under3s/30under5s/15under8s. Rebuild usesjudgment-firstfixedrubricswithoutspeedbonuses. LegacyNetlifyleaderboard acceptssuppliedscore withnamevalidation, noauthoritativesessionproof; keepseparatelegacycomparisonsandnewself-reportedlocalresults. Legacygame-room backend needsNetlifyBlobsconfiguration; preserveexistingcode/records, isolateinlegacyentry and donotadvertisefakeonlineplayers.
+1. Complete browser rounds for the newly integrated Quick Check bank, timed Quick Check, Daily Dispatch (including midnight/reload/resume), and representative remaining Case Files with chart/table/timeline interactions.
+2. Finish shared-challenge reconstruction: curated Quick/Daily/review round links must reproduce exact question IDs and target overrides. Those outgoing friend buttons are withheld in this checkpoint; ordinary fixed Case/Mission and ordinary seeded Atlas links remain. Validate incoming versions, content-edition recovery, and daily resume behavior before enabling the withheld links.
+3. Add the finale's actual sensitivity range to the readable standalone briefing. It is currently visible in the investigation and retained in the exported numerical record; the briefing's prose/tables do not yet present that range as a separate section.
+4. Finish accessibility and performance review of the integrated shell at 375/390/768/1280/1440 widths, enlarged text and reduced motion. Actual assistive-technology, physical touch and CPU/network-throttling tests have not been performed. Improve active-simulation save frequency and bounded asset-loader failure recovery.
+5. Review skill/review attribution: journal currently describes tagged rounds and overall round scores honestly; per-item skill accuracy and missed-rationale review can be refined. Finish consolidated maintenance/performance/content/asset inventory and publisher editorial review.
+6. Rebuild and run the complete release checks, then prepare the final release artifact. This is a working checkpoint, not a claim that all release acceptance criteria have passed.
 
-## First integrated slice — played and improved
+## Model and content boundaries
 
-Root completed the two-stage Case File through the shipped app: first insufficient-evidence verdict40/40, refreshed and retained that score, revised to false after the new archive records60/60, saved100/100. A third evidence selection was rejected. Compact three-step decision form and local-anchor navigation were repaired.
-
-Root completed The map everyone shared through the shipped app: computed a400m buffer containing Old Town, ran maintained drainage3600s and restricted drainage3600s, saved named runs, computed/pinned the actual one-factor depth comparison (maximum difference~0.039m), refreshed and retained model/snapshots, chose bounded uncertainty, selected relevant document+GIS+comparison and two limitations, then saved a100/100 briefing. Independent review found/fixed GIS fingerprint context mismatch, masked-observation coordinate linkage, stale probe selection, worker-reply races, display precision and hidden observation invariants. Further polish/regression remains.
-
-Atlas agent completed ten-target Expedition via identical game UI harness (930/1000 including wrong answer, hint and unscored revisit), Classic50 andTyping50, plus Hard50 andReasoning10 through the shipped wrapper. Reload preserved exact target, partially typed answer, camera and results. Full Hard completed with49mapclicks+1numericcoordinate selection.375/390/768nooverflow. Evidence inventory in tests/games/atlas-browser-evidence.json.
-
-The required initial playable slice has been completed and improved. Full content expansion is now authorized by the brief’s sequence. Next: finish6missions/6cases/24quick items; play every mode through final app, strengthen persistence/versioning, finish visual/accessibility review and package complete release.
+The physical scene is a local 1200×800-metre educational world on 50-metre cells, distinct from the real geographic Atlas. All case records are original fictional educational material. Observations remain immutable across counterfactual experiments. Numerical assumptions, conservation/convergence results, reproducible case paths, data provenance and licenses are documented in docs/games-model.md, docs/games-validation.md, docs/games-content.md and docs/games-atlas.md. Human editorial review remains pending.
diff --git a/docs/games-content.md b/docs/games-content.md
--- a/docs/games-content.md
+++ b/docs/games-content.md
@@ -38,6 +38,8 @@
 
 All numbers in the short cases are authored fictional source material, not asserted output from the interactive simulation. The long missions instead bind values generated by the numerical engine. Optional chart, citation, table and timestamp metadata support inspectable evidence views without replacing the full source text.
 
+The journal's “Practice by topic” section groups completed rounds by their content tags. Its percentage is the combined overall score of those tagged rounds, including their other topics. It is explicitly not an individual skill-accuracy metric; per-item skill attribution is not implemented.
+
 ## Maintenance
 
 See `games/CONTENT-CONTRACT.md` for field names, provenance rules, engine parameter names and observation binding. Content lives independently of presentation in `games/content.js`. Give every record a stable ID and explicit version; do not change a live attempt's content snapshot. Increment a substantive content version when its evidence or rubric changes. Do not retroactively judge an earlier stage against evidence that arrived later.
diff --git a/docs/games-validation.md b/docs/games-validation.md
--- a/docs/games-validation.md
+++ b/docs/games-validation.md
@@ -33,4 +33,4 @@
 - Case two: maintained and optional-link 3600 s runs were saved through the UI; the directed optional path reported 761.58 m along `works-wharf`. The initial East Wharf 2400 s concentration remained 0.11466336 g/m³. Locking Old Town as the additional measurement revealed 1800 s concentration 0.00027181 g/m³. The inventory source, actual network pin, one-factor comparison, bounded supported conclusion and two limitations produced 100/100. The final briefing saved to the journal. No console errors were recorded during this pass.
 - Rainfall over time: the sandbox UI accepted 0 mm/h during 0–600 s followed by 100 mm/h during 600–1200 s, then dry conditions. Its full-hour numerical ledger reported 16000.000000000027 m³ of rainfall. The independent area×rate×duration calculation is 960000 m²×0.1 m/h×(600/3600 h)=16000 m³. The temporary viewport override was reset after responsive review.
 
-One presentation defect was reported: the case-two comparison summary uses the rubric's East Wharf probe, while its initial notebook plot followed the independently selected Old Town location without naming the difference. Scoring and observed residuals used the correct probe. The controller owner is correcting the graph context; browser verification of that repair is pending. Cases three through six have automated full-score paths above; this independent browser pass does not claim they were all played manually.
+The reported notebook presentation defect is repaired and freshly verified in the browser. The comparison now says which location its graph follows and separately names the probes in its peak table. After reloading the current code, changing the inspected location from Old Town to East Wharf changed the caption from “Graph follows Old Town” to “Graph follows East Wharf” and updated the actual plotted depth scale, while the comparison's Old Town peak table retained its explicit location label. This final focused check used two saved copies of an existing sandbox state; it was not a new causal experiment or scoring claim. Cases three through six have automated full-score paths above; this independent browser pass does not claim they were all played manually.
diff --git a/games/app.js b/games/app.js
--- a/games/app.js
+++ b/games/app.js
@@ -21,7 +21,7 @@
  function savedFor(kind,id){const matches=run=>run?.kind===kind&&(kind==='case'?run.caseId===id:kind==='mission'?run.missionId===id:kind==='atlas'||kind==='quick'?run.mode===id:true)&&run.phase!=='complete'&&(!activeShare().get('seed')||String(run.seed)===activeShare().get('seed'));return matches(book.active)?book.active:Object.values(book.drafts).find(matches)||null;}
  function activeShare(){return location.hash===sharedRoute?new URL(location.href).searchParams:new URLSearchParams();}
  function validShare(expected){const q=activeShare();if(!q.get('seed')||(!q.get('rules')||q.get('rules')===S.VERSION)&&(!q.get('content')||q.get('content')===expected))return true;main.innerHTML=titleRow('Challenge edition unavailable','This link uses different game rules.','Comparable results require the same rules and content edition. Your current notebook is preserved.')+'<button class="g-button g-button-primary" data-current-edition>Start with the current edition</button>';main.querySelector('[data-current-edition]').onclick=()=>{const url=new URL(location.href);url.search='';history.replaceState(null,'',url);renderRoute();};return false;}
- function resultView(result,run){lastResult={result,run};const percent=Math.round(result.score/Math.max(1,result.maxScore)*100);main.innerHTML=titleRow('Saved in your journal','The record is stronger now.','This is a local game result, not a verified global ranking.')+`<section class="g-panel"><div class="g-row" style="justify-content:space-between"><div><div class="g-kicker">${E(result.mode||run.kind)}</div><div class="journal-stat">${result.score} <span class="g-note">/ ${result.maxScore}</span></div></div><span class="g-chip g-chip-teal">${percent}% of available points</span></div><div class="progress-track"><span style="width:${percent}%"></span></div><p class="g-note">${result.total?`${result.correct} of ${result.total} completed judgments or targets were correct. `:''}Hints and revised decisions remain part of your saved record.</p>${result.breakdown?`<div class="score-breakdown">${Object.entries(result.breakdown).map(([k,v])=>`<div><strong>${E(v)}</strong><span>${E(k)}</span></div>`).join('')}</div>`:''}${result.explanation?`<p>${E(result.explanation)}</p>`:''}<div class="g-row"><a class="g-button g-button-primary" href="#/journal">Open your journal →</a><a class="g-button" href="#/hub">Back to Games</a><button type="button" class="g-button" data-share>Challenge a friend ↗</button><button type="button" class="g-button" data-card>Save result card</button>${result.briefing?'<button class="g-button g-button-primary" type="button" data-read-brief>Read your briefing →</button><button class="g-button" type="button" data-brief>Save briefing document ↓</button>':''}</div><p class="g-note" style="margin-top:16px">Friend comparisons are casual and self-reported. A shared link contains the same seed and version, without answers.</p></section>`;main.querySelector('[data-share]').onclick=()=>share(run,result);main.querySelector('[data-card]').onclick=()=>resultCard(run,result);main.querySelector('[data-read-brief]')?.addEventListener('click',()=>showBriefing(result.briefing));main.querySelector('[data-brief]')?.addEventListener('click',()=>showBriefing(result.briefing,true));}
+ function resultView(result,run){lastResult={result,run};const canShare=!run.dailyPlan&&!run.reviewMode&&(run.kind==='case'||run.kind==='mission'||(run.kind==='atlas'&&!run.targets));const percent=Math.round(result.score/Math.max(1,result.maxScore)*100);main.innerHTML=titleRow('Saved in your journal','The record is stronger now.','This is a local game result, not a verified global ranking.')+`<section class="g-panel"><div class="g-row" style="justify-content:space-between"><div><div class="g-kicker">${E(result.mode||run.kind)}</div><div class="journal-stat">${result.score} <span class="g-note">/ ${result.maxScore}</span></div></div><span class="g-chip g-chip-teal">${percent}% of available points</span></div><div class="progress-track"><span style="width:${percent}%"></span></div><p class="g-note">${result.total?`${result.correct} of ${result.total} completed judgments or targets were correct. `:''}Hints and revised decisions remain part of your saved record.</p>${result.breakdown?`<div class="score-breakdown">${Object.entries(result.breakdown).map(([k,v])=>`<div><strong>${E(v)}</strong><span>${E(k)}</span></div>`).join('')}</div>`:''}${result.explanation?`<p>${E(result.explanation)}</p>`:''}<div class="g-row"><a class="g-button g-button-primary" href="#/journal">Open your journal →</a><a class="g-button" href="#/hub">Back to Games</a>${canShare?'<button type="button" class="g-button" data-share>Challenge a friend ↗</button>':''}<button type="button" class="g-button" data-card>Save result card</button>${result.briefing?'<button class="g-button g-button-primary" type="button" data-read-brief>Read your briefing →</button><button class="g-button" type="button" data-brief>Save briefing document ↓</button>':''}</div><p class="g-note" style="margin-top:16px">${canShare?'Friend comparisons are casual and self-reported. A shared link contains the same seed and version, without answers.':'Your local result and result card are available. Friend links for curated rounds are pending a check that they reproduce the exact same questions.'}</p></section>`;main.querySelector('[data-share]')?.addEventListener('click',()=>share(run,result));main.querySelector('[data-card]').onclick=()=>resultCard(run,result);main.querySelector('[data-read-brief]')?.addEventListener('click',()=>showBriefing(result.briefing));main.querySelector('[data-brief]')?.addEventListener('click',()=>showBriefing(result.briefing,true));}
  function finish(run,result){if(book.receipts[run.id]){const saved=book.history.find(x=>x.runId===run.id);resultView(saved?{...saved,briefing:saved.briefing||book.briefings.find(b=>b.runId===saved.briefingRef)}:result,run);return;}book=S.complete(book,run,result);save();UI.sound(book.settings,'correct');resultView(result,run);UI.announce('Result saved in your journal.');}
  async function showBriefing(brief,download=false){if(brief){const m=C.getMission(brief.missionId);brief={...brief,conclusionText:brief.conclusionText||m?.conclusions.find(c=>c.id===brief.answer?.conclusionId)?.label,uncertaintyTexts:brief.uncertaintyTexts||m?.uncertaintyChoices.filter(c=>brief.answer?.uncertaintyIds?.includes(c.id)).map(c=>c.label)};}if(!brief){UI.announce('This saved briefing is unavailable. The score receipt is retained.');return;}try{UI.style('/games/briefing.css');await UI.script('/games/briefing.js');if(download)UI.download('noteworthy-'+brief.missionId+'-briefing.html',window.NoteworthyBriefing.document(brief),'text/html');else UI.modal('Your filed investigation',`<div class="g-row"><button class="g-button" data-save-document>Save briefing document ↓</button><button class="g-button" data-save-record>Export numerical record ↓</button></div>`+window.NoteworthyBriefing.render(brief),d=>{d.querySelector('[data-save-document]').onclick=()=>showBriefing(brief,true);d.querySelector('[data-save-record]').onclick=()=>UI.download('noteworthy-'+brief.missionId+'-evidence.json',brief);});}catch{UI.announce('The briefing view could not load. You can export the full notebook from Journal.');}}
  function challengeURL(run){const url=new URL(location.href);url.pathname='/games/';url.search='';url.searchParams.set('seed',run.seed);url.searchParams.set('rules',S.VERSION);url.searchParams.set('content',run.contentVersion);url.hash=routeFor(run).slice(1);return url.href;}
@@ -33,7 +33,7 @@
  async function launchMission(id,sandbox,token){UI.style('/games/investigation.css');UI.style('/games/investigation-map.css');for(const file of ['physics','world','gis','mission-rubric','investigation-view','investigation'])await UI.script('/games/'+file+'.js');if(token!==transition)return;if(!validShare(C.version))return;const mission=sandbox?null:C.getMission(id);if(!sandbox&&!mission){notFound();return;}let run=savedFor(sandbox?'sandbox':'mission',id)||S.newRun(sandbox?'sandbox':'mission',{missionId:id,seed:activeShare().get('seed')||id||'sandbox',contentVersion:C.version});setActive(run);controller=window.NoteworthyInvestigation.mount(main,{mission,sandbox,saved:run,settings:book.settings,onSave:next=>{run=next;setActive(run);},onFinish:result=>finish(run,result)});}
  function journal(){const drafts=[book.active,...Object.values(book.drafts)].filter((r,i,a)=>r&&a.findIndex(x=>x?.id===r.id)===i);main.innerHTML=titleRow('Your field journal','Progress with a memory.','Saved locally. These skill labels describe game practice, not a validated assessment of real-world ability.','<button class="g-button" type="button" data-export>Export notebook ↓</button>')+`<div class="g-grid-three"><div class="g-panel"><div class="journal-stat">${book.history.length}</div><p class="g-note">Completed runs</p></div><div class="g-panel"><div class="journal-stat">${book.briefings.length}</div><p class="g-note">Saved investigation briefings</p></div><div class="g-panel"><div class="journal-stat">${Object.keys(book.mastery).length}</div><p class="g-note">Places practiced</p></div></div>${drafts.length?`<section class="g-panel" style="margin-top:20px"><h2>Continue an unfinished run.</h2><ul class="journal-list">${drafts.map(r=>`<li><span>${E(runLabel(r))}</span><button class="g-button g-button-small" data-resume="${E(r.id)}">Continue →</button></li>`).join('')}</ul></section>`:''}<section class="g-panel" style="margin-top:20px"><h2>Practice that stays with you.</h2><p class="g-note" style="margin-top:12px">Independent recall requires a separate run in the same mode at least 24 hours later. An immediate repeat does not count as durable recall.</p>${Object.keys(book.mastery).length?`<ul class="journal-list">${Object.entries(book.mastery).map(([id,entry])=>`<li><strong>${E(window.NoteworthyAtlasData?.countries.find(x=>x.id===id)?.name||id)}</strong><span>${E(entry.label)}</span></li>`).join('')}</ul>`:'<p class="g-note">Your expedition journal will appear after a completed journey.</p>'}</section><section class="g-panel" style="margin-top:20px"><h2>Review what challenged you.</h2>${book.review.length?`<ul class="journal-list">${book.review.slice(-15).map(x=>`<li><span>${E(x.id)}</span><button class="g-button g-button-small" data-review-kind="${E(x.kind)}" data-review-id="${E(x.id)}">Practice again →</button></li>`).join('')}</ul>`:'<p class="g-note" style="margin-top:12px">Missed and assisted items will appear here.</p>'}</section>${progressPanels()}<section class="g-panel" style="margin-top:20px"><h2>Milestones earned in play.</h2><div class="g-row" style="margin-top:16px">${book.achievements.length?book.achievements.map(a=>`<span class="g-chip g-chip-teal">${E({'careful-reader':'A complete, careful round','returned-and-recalled':'Returned and recalled','filed-with-evidence':'Filed with evidence'}[a]||a)}</span>`).join(''):'<p class="g-note">Your first completed investigation is a good place to start.</p>'}</div></section><section class="g-panel" style="margin-top:20px"><h2>The results shelf.</h2>${book.history.length?`<ul class="journal-list">${book.history.map(r=>`<li><div><strong>${E(runLabel(r))}</strong><div class="g-note">${E(new Date(r.finishedAt).toLocaleDateString())} · ${E(r.contentVersion)}</div></div><span>${r.score} / ${r.maxScore}</span>${r.briefing||r.briefingRef?`<button class="g-button g-button-small" data-brief="${E(r.runId)}">Briefing ↓</button>`:''}</li>`).join('')}</ul>`:'<p class="g-note" style="margin-top:12px">Completed results appear here. There are no invented scores or players.</p>'}</section>`;main.querySelector('[data-export]').onclick=()=>UI.download('noteworthy-games-notebook.json',book);main.querySelectorAll('[data-review-kind]').forEach(b=>b.onclick=()=>startReview(b.dataset.reviewKind,b.dataset.reviewId));main.querySelectorAll('[data-resume]').forEach(b=>b.onclick=()=>{const run=drafts.find(x=>x.id===b.dataset.resume);setActive(run);go(routeFor(run));});main.querySelectorAll('[data-brief]').forEach(b=>b.onclick=()=>showBriefing(book.briefings.find(x=>x.runId===b.dataset.brief))); }
  function runLabel(r){return C.getMission(r.missionId)?.title||C.getCase(r.caseId)?.title||({'expedition':'Atlas Expedition','classic':'Classic 50','hard':'Hard 50','typing':'Typing 50','reasoning':'Location reasoning','quick-check':'Quick Check','quick-timed':'Timed Quick Check','daily-dispatch':'Today’s Dispatch','sandbox':'Merehaven laboratory','case-files':'Case File','integrated-investigation':'Field investigation','expedition-review':'Atlas review'}[r.mode||r.kind])||r.mode||r.kind;}
- function progressPanels(){const done=new Set(book.history.map(r=>r.missionId).filter(Boolean)),skills={};for(const r of book.history.filter(x=>x.kind==='case'||x.kind==='quick'))for(const tag of r.skillTags||[]){skills[tag]=skills[tag]||{runs:0,score:0,max:0};skills[tag].runs++;skills[tag].score+=r.score;skills[tag].max+=r.maxScore;}return `<section class="g-panel" style="margin-top:20px"><h2>The season in your notebook.</h2><ul class="journal-list">${C.chapters.map(ch=>`<li><strong>${E(ch.title)}</strong><span>${ch.missionIds.filter(id=>done.has(id)).length} / ${ch.missionIds.length} briefings filed</span></li>`).join('')}</ul></section><section class="g-panel" style="margin-top:20px"><h2>Evidence skills in practice.</h2><p class="g-note">These describe your completed game decisions, not a validated assessment of reporting ability.</p>${Object.keys(skills).length?`<ul class="journal-list">${Object.entries(skills).map(([tag,s])=>`<li><strong>${E(tag.replaceAll('-',' '))}</strong><span>${s.runs} rounds · ${Math.round(s.score/s.max*100)}% rubric points</span></li>`).join('')}</ul>`:'<p class="g-note">Complete a Case File or Quick Check to begin a record of source tracing, dates, images and statistics.</p>'}</section><section class="g-panel" style="margin-top:20px"><h2>Your personal bests.</h2><p class="g-note">Rules, content edition and input category stay separate.</p>${Object.keys(book.bests).length?`<ul class="journal-list">${Object.entries(book.bests).map(([key,value])=>`<li><span>${E(key.replaceAll(':',' · '))}</span><strong>${value} points</strong></li>`).join('')}</ul>`:'<p class="g-note">Your first completed run will establish a personal best.</p>'}</section>`;}
+ function progressPanels(){const done=new Set(book.history.map(r=>r.missionId).filter(Boolean)),skills={};for(const r of book.history.filter(x=>x.kind==='case'||x.kind==='quick'))for(const tag of r.skillTags||[]){skills[tag]=skills[tag]||{runs:0,score:0,max:0};skills[tag].runs++;skills[tag].score+=r.score;skills[tag].max+=r.maxScore;}return `<section class="g-panel" style="margin-top:20px"><h2>The season in your notebook.</h2><ul class="journal-list">${C.chapters.map(ch=>`<li><strong>${E(ch.title)}</strong><span>${ch.missionIds.filter(id=>done.has(id)).length} / ${ch.missionIds.length} briefings filed</span></li>`).join('')}</ul></section><section class="g-panel" style="margin-top:20px"><h2>Practice by topic.</h2><p class="g-note">Each topic groups completed rounds carrying that tag. The percentage combines those rounds’ overall scores, including their other topics; it does not measure accuracy in this individual skill.</p>${Object.keys(skills).length?`<ul class="journal-list">${Object.entries(skills).map(([tag,s])=>`<li><strong>${E(tag.replaceAll('-',' '))}</strong><span>${s.runs} tagged rounds · ${Math.round(s.score/s.max*100)}% overall round score</span></li>`).join('')}</ul>`:'<p class="g-note">Complete a Case File or Quick Check to begin a record of source tracing, dates, images and statistics.</p>'}</section><section class="g-panel" style="margin-top:20px"><h2>Your personal bests.</h2><p class="g-note">Rules, content edition and input category stay separate.</p>${Object.keys(book.bests).length?`<ul class="journal-list">${Object.entries(book.bests).map(([key,value])=>`<li><span>${E(key.replaceAll(':',' · '))}</span><strong>${value} points</strong></li>`).join('')}</ul>`:'<p class="g-note">Your first completed run will establish a personal best.</p>'}</section>`;}
  async function startReview(kind,id){if(kind==='case'){go('#/case/'+id);return;}if(kind==='atlas'){await atlasAssets();const all=window.NoteworthyAtlasData.countries.map(c=>c.id),misses=book.review.filter(r=>r.kind==='atlas'&&all.includes(r.id)).map(r=>r.id),targets=[...new Set([id,...misses,...S.shuffle(all,'review-'+Date.now())])].slice(0,10);setActive(S.newRun('atlas',{mode:'expedition',contentVersion:window.NoteworthyAtlasData.version,seed:'review-'+Date.now(),targets,reviewMode:true}));go('#/atlas/expedition');}else{const ids=[id,...S.selectBank(C.quickChecks.filter(q=>q.id!==id),4,'review-'+Date.now(),[])].filter(id=>C.getQuick(id));setActive(S.newRun('quick',{mode:'review',contentVersion:C.version,seed:'review-'+Date.now(),ids,index:0,stageIndex:0,answers:{},selection:{evidenceIds:[],confidence:'unsure'}}));go('#/quick/review');}}
  function legacy(){main.innerHTML=titleRow('Preserved records','The original games are still here.','Legacy scores use their original rules and remain separate from the new collection.')+`<section class="g-panel"><h2>Legacy play and score records</h2><p class="g-note" style="margin-top:15px">The previous game code, save keys and services have been retained. New results are saved locally and never submitted under the old scoring rules. The legacy server accepts player-submitted scores; they are not equivalent to a verified competition.</p><div class="g-row"><a class="g-button" href="/games/legacy-fact-checker.html">Original Fact Checker ↗</a><a class="g-button" href="/games/legacy-geography.html">Original Geography ↗</a><button class="g-button" type="button" data-load-scores>Load legacy public scores</button></div><p class="g-note" style="margin-top:18px">Legacy multiplayer is not enabled in the rebuilt solo collection. Its existing service code and records are preserved; an unavailable secondary service cannot block a solo game.</p><div id="legacy-scores"></div></section>`;main.querySelector('[data-load-scores]').onclick=async event=>{const button=event.currentTarget,target=main.querySelector('#legacy-scores');button.disabled=true;target.innerHTML='<p class="g-note">Loading the existing public score list…</p>';const abort=new AbortController(),timer=setTimeout(()=>abort.abort(),5000);try{const response=await fetch('/.netlify/functions/leaderboard?gameType=fact-checker&limit=10',{signal:abort.signal});if(!response.ok)throw Error('service unavailable');const data=await response.json();if(!Array.isArray(data.scores))throw Error('unreadable response');target.innerHTML=data.scores.length?`<table><caption>Legacy Fact Checker · Self-reported scores</caption><thead><tr><th>Player</th><th>Legacy score</th></tr></thead><tbody>${data.scores.map(r=>`<tr><td>${E(r.userName||'Unnamed player')}</td><td>${E(r.score)}</td></tr>`).join('')}</tbody></table>`:'<p class="g-note">No legacy scores were returned.</p>';}catch{target.innerHTML='<p class="g-note">Legacy scores are unavailable in this preview. Your solo progress is unaffected.</p>';}finally{clearTimeout(timer);button.disabled=false;}};}
  function notFound(){main.innerHTML='<section class="g-empty"><h1>This case could not be found.</h1><p>Your notebook remains saved. Choose a current game to continue.</p><a class="g-button" href="#/hub">Open Noteworthy Games →</a></section>';}
diff --git a/games/investigation.js b/games/investigation.js
--- a/games/investigation.js
+++ b/games/investigation.js
@@ -18,7 +18,7 @@
   function opts(items,value){return items.map(x=>`<option value="${E(x.id)}"${x.id===value?' selected':''}>${E(x.name||x.title||x.label||x.id)}</option>`).join('');}
   function field(label,name,value,min,max,step=1){return `<label class="g-field">${label}<input name="${name}" type="number" value="${value}" min="${min}" max="${max}" step="${step}" required></label>`;}
   function select(label,name,items,value){return `<label class="g-field">${label}<select name="${name}">${opts(items,value)}</select></label>`;}
-  function notice(message){UI.announce(message);const target=container.querySelector('[data-model-message]');if(target)target.textContent=message;}
+  function notice(message,announce=true){if(announce)UI.announce(message);const target=container.querySelector('[data-model-message]');if(target)target.textContent=message;}
   function tolerance(){return desk.variable==='concentration'?(mission?.observationBinding.concentrationToleranceGm3||.000001):(mission?.observationBinding.depthToleranceM||.001);}
   function selectedObservationTable(){const visible=rows(),variables=mission?.observationBinding.variables||['depth'],precision=variables.map(v=>v==='depth'?`depth ±${num(mission.observationBinding.depthToleranceM||.001,6)} m`:`tracer ±${num(mission.observationBinding.concentrationToleranceGm3||.000001,8)} g/m³`).join('; ');return visible.length?`<table><caption>Saved synthetic field record · Fixed values · ${precision}</caption><thead><tr><th>Location</th><th>Time (s)</th>${variables.map(v=>`<th>${v==='depth'?'Depth (m)':'Tracer (g/m³)'}</th>`).join('')}</tr></thead><tbody>${visible.map(x=>`<tr><th>${E(feature(x.featureId).name)}</th><td>${x.t}</td>${variables.map(v=>`<td>${num(x[v],8)}</td>`).join('')}</tr>`).join('')}</tbody></table>`:'<p class="g-note">Preparing the saved synthetic record…</p>';}
   function brief(){return `<div class="g-kicker">The claim to investigate</div><h2>${E(mission?.claim||'Change the inputs. Follow the consequences.')}</h2><p>${E(mission?.briefing||'Experiment freely in fictional Merehaven. Water follows terrain and the drainage network; harmless tracer travels with it. Save named runs and compare a single change. This sandbox has no competitive score.')}</p>${mission?`<ol class="inv-guide">${mission.guidedSteps.map((s,i)=>`<li><strong>${E(s.title)}</strong><p>${E(s.instruction)}</p></li>`).join('')}</ol>`:'<p class="g-note">Start with maintained drainage, then test a storm, a blocked outlet, a new connection or a raised terrain cell.</p>'}${button('Measure the map →','tab-spatial',true)}<details><summary>How this physical world works</summary><p>Rain adds water to each terrain cell. Some soaks into the surface; the rest moves downhill or through enabled drains. The harmless tracer is carried with that moving water and spreads between neighboring cells. Change a drain and both the water and tracer graphs respond.</p><p class="g-note">A 50 m grid simplifies the ground. Drain links transfer water without resolving pipe travel time. Simulated readings share the model’s assumptions.</p><a class="g-link" href="/docs/games-model.md" target="_blank" rel="noopener">Read equations, limits and validation ↗</a></details>${mission?`<details class="inv-observed"><summary>Read the saved field record</summary>${selectedObservationTable()}<p class="g-note">Original fictional educational data, generated once from the fixed model. Counterfactual experiments cannot edit these observations.</p></details>`:''}`;}
@@ -36,8 +36,8 @@
   function render(){if(dead)return;const tabs=[['brief','Brief'],['spatial','Map tools'],['model','Model'],['notebook','Notebook'],...(!sandbox?[['filing','Briefing']]:[])];container.innerHTML=`<div class="inv-heading"><div><div class="g-kicker">The Situation Room / ${sandbox?'Open laboratory':E(mission.chapterId.replace('chapter-','Chapter '))}</div><h1>${sandbox?'Merehaven laboratory':E(mission.title)}</h1><p class="g-note">Fictional investigation · Coupled water and harmless tracer · Saved on this device</p></div><a href="#/campaign" class="g-button g-button-small">All cases</a></div><div class="inv-mobile-switch" aria-label="Workspace view">${button('Map & instruments','mobile-map')}${button('Investigation desk','mobile-desk')}</div><div class="inv-workspace" data-mobile-view="${desk.mobileView||'map'}"><section class="inv-map-column" aria-label="Merehaven model workspace"><div class="inv-map-bar"><span class="g-chip">MODEL PREDICTION</span><span data-clock>${UI.time(state.t)} / 60:00</span><span>${E(state.config.name)}</span></div><div data-map></div><div class="inv-map-controls">${button('−','zoom-out')}${button('+','zoom-in')}${button('Reset map','reset-map')}${select('Inspect location','location',[...W.features,...state.config.probes.filter(p=>!W.features.some(f=>f.id===p.id)).map(p=>({...p,name:p.id}))],desk.selected)}<p class="g-note">Drag to pan · Arrow keys pan · +/− zoom. Metres, north up.</p></div><div class="inv-playback">${button(timer?'Pause':'Run','play',true)}${button('Step 60 s','step')}${button('Rewind','rewind')}${button('Run to 60:00','end')}${select('Playback speed','speed',[{id:'1',name:'1×'},{id:'4',name:'4×'},{id:'12',name:'12×'}],String(desk.speed))}</div><div class="inv-timeline"><label class="g-field">Inspect model time (seconds)<input aria-label="Inspect model time in seconds" type="range" min="0" max="3600" step="60" value="${Math.round(state.t/60)*60}" data-time></label><output data-time-label>${Math.round(state.t)} s</output></div><p class="g-note inv-message" data-model-message>${busy?'Computing the physical model…':'Prediction clock is paused. Field observations remain fixed.'}</p><div class="inv-linked"><div class="g-row">${select('Graph variable','variable',[{id:'depth',name:'Water depth (m)'},{id:'concentration',name:'Tracer concentration (g/m³)'}],desk.variable)}<span class="g-note" data-reading></span></div><div data-graph></div><details><summary>Selected feature attributes</summary><div data-attributes></div></details><details><summary>Water and tracer conservation</summary><pre data-budget></pre></details></div></section><section class="inv-desk" aria-label="Investigation desk"><div class="inv-tabs" role="tablist" aria-label="Investigation stages">${tabs.map(([id,label])=>`<button type="button" role="tab" aria-selected="${desk.tab===id}" data-tab="${id}" id="tab-${id}" aria-controls="inv-panel" tabindex="${desk.tab===id?'0':'-1'}">${label}</button>`).join('')}</div><div class="inv-desk-content" id="inv-panel" role="tabpanel" aria-labelledby="tab-${desk.tab}">${desk.tab==='brief'?brief():desk.tab==='spatial'?spatial():desk.tab==='model'?model():desk.tab==='notebook'?notebook():filing()}</div>${mission?`<details class="inv-documents"><summary>Source documents (${mission.documents.length})</summary>${mission.documents.map(d=>`<article><div class="g-kicker">${E(d.kind.replaceAll('-',' '))}</div><h3>${E(d.title)}</h3><p>${E(d.body)}</p><p class="g-note">${E(d.provenance.label)} · Human editorial review pending</p><button type="button" class="g-button g-button-small" data-document-location="${E(d.featureId)}">Locate this record on map</button></article>`).join('')}</details>`:''}</section></div>`;bind();updateModel();}
   function updateModel(){if(dead)return;if(!state.config.probes.some(p=>p.id===desk.selected))desk.selected='old-town';container.querySelector('[data-map]').innerHTML=V.map({state,world:W,layers:desk.layers,selectedFeatureId:desk.selected,analysis:desk.analyses.at(-1),probes:state.config.probes.filter(x=>!W.features.some(f=>f.id===x.id)),observations:rows(),viewBox:desk.viewBox,suffix:'workspace'});container.querySelector('[data-clock]').textContent=UI.time(state.t)+' / 60:00';container.querySelector('[data-time]').value=Math.round(state.t/60)*60;container.querySelector('[data-time-label]').textContent=Math.round(state.t)+' s';const sample=P.sample(state,desk.selected);container.querySelector('[data-reading]').textContent=`${num(sample.depth)} m depth · ${num(sample.concentration,6)} g/m³ at ${Math.round(state.t)} s`;container.querySelector('[data-graph]').innerHTML=state.t===0?'<div class="inv-graph-empty"><strong>The experiment starts here.</strong><p>Run or step the model to follow this probe through time. Its current reading is shown above.</p></div>':V.graph({series:[{name:'Current model prediction',color:'#69d5c6',rows:state.sensorHistory[desk.selected]||[]}],observations:rows(),variable:desk.variable,probeId:desk.selected});const selected=state.config.probes.find(p=>p.id===desk.selected),zone=selected?W.zones.find(z=>G.contains(z,[selected.x,selected.y])):null;container.querySelector('[data-attributes]').innerHTML=`<dl><dt>Location</dt><dd>${E(feature(desk.selected).id===desk.selected?feature().name:desk.selected)}</dd><dt>Coordinates</dt><dd>East ${num(selected?.x,1)} m; north ${num(selected?.y,1)} m</dd><dt>Land-use polygon</dt><dd>${E(zone?.name||'Outside named land-use polygons')}</dd><dt>Fictional residents in this polygon</dt><dd>${zone?zone.properties.fictionalPopulation:'Not assigned'}</dd><dt>Configured permeability at this cell</dt><dd>${num(state.config.permeability[sample.index],2)} mm/h</dd><dt>Current model time</dt><dd>${state.t} seconds</dd></dl>`;container.querySelector('[data-budget]').textContent=JSON.stringify(P.summary(state),null,2);bindMap();if(desk.tab==='notebook'){const target=container.querySelector('[data-comparison]');if(target){target.innerHTML=comparisonOutput(desk.comparisons.at(-1));target.querySelector('[data-action="compare-maps"]')?.addEventListener('click',()=>action('compare-maps'));}}}
   function pause(){if(timer){clearTimeout(timer);timer=null;}const b=container.querySelector('[data-action="play"]');if(b)b.textContent='Run';save();}
-  async function advance(until,replay=false){if(busy||dead)return;const revision=modelRevision;busy=true;notice('Computing the physical model…');try{const next=await compute(replay?'replay':'run',replay?{config:state.config,options:{until,dt:5}}:{state,options:{until,dt:5,sampleEvery:60}});if(dead||revision!==modelRevision)return;state=next;save();updateModel();notice(`Model prediction at ${Math.round(state.t)} seconds. Saved observations unchanged.`);}catch(e){notice(e.message);}finally{busy=false;}}
-  async function tick(){if(dead||!timer)return;await advance(Math.min(3600,state.t+15*desk.speed));if(state.t>=3600){pause();return;}if(timer)timer=setTimeout(tick,180);}
+  async function advance(until,replay=false){if(busy||dead)return;const revision=modelRevision;busy=true;notice('Computing the physical model…',!timer);try{const next=await compute(replay?'replay':'run',replay?{config:state.config,options:{until,dt:5}}:{state,options:{until,dt:5,sampleEvery:60}});if(dead||revision!==modelRevision)return;state=next;save();updateModel();notice(`Model prediction at ${Math.round(state.t)} seconds. Saved observations unchanged.`,!timer);}catch(e){notice(e.message);}finally{busy=false;}}
+  async function tick(){if(dead||!timer)return;await advance(Math.min(3600,state.t+15*desk.speed));if(state.t>=3600){pause();notice('Model run complete at 3600 seconds. Saved observations unchanged.');return;}if(timer)timer=setTimeout(tick,180);}
   function newConfig(config,label){pause();modelRevision++;state=P.create(config);log('New experiment',label);save();render();notice('Inputs applied. Prediction reset to zero; saved evidence retained.');}
   function addProbe(x,y){pause();modelRevision++;const id='probe-'+Math.round(x)+'-'+Math.round(y);state=P.withProbe(state,{id,x,y});desk.selected=id;log('Virtual probe',`${id}: east ${x} m, north ${y} m. Sampling does not change physics.`);save();render();}
   function pan(dx,dy){const v=desk.viewBox;v[0]=Math.max(0,Math.min(1200-v[2],v[0]+dx));v[1]=Math.max(0,Math.min(800-v[3],v[1]+dy));save();updateModel();}
diff --git a/games/shared.js b/games/shared.js
--- a/games/shared.js
+++ b/games/shared.js
@@ -7,7 +7,7 @@
  function download(name,data,type='application/json'){const blob=new Blob([typeof data==='string'?data:JSON.stringify(data,null,2)],{type});const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}
  function time(seconds){const t=Math.max(0,Math.floor(seconds||0));return String(Math.floor(t/60)).padStart(2,'0')+':'+String(t%60).padStart(2,'0');}
  function closeDialog(dialog){dialog.close();dialog.remove();}
- function modal(title,body,onReady){const dialog=document.createElement('dialog');dialog.className='g-dialog';dialog.innerHTML='<div class="g-dialog-heading"><h2>'+esc(title)+'</h2><button type="button" class="g-icon-button" aria-label="Close dialog">×</button></div>'+body;dialog.querySelector('button').onclick=()=>closeDialog(dialog);dialog.addEventListener('cancel',event=>{event.preventDefault();closeDialog(dialog);},{once:true});document.body.append(dialog);dialog.showModal();onReady?.(dialog);return dialog;}
+ function modal(title,body,onReady){const dialog=document.createElement('dialog');dialog.className='g-dialog';dialog.setAttribute('aria-label',title);dialog.innerHTML='<div class="g-dialog-heading"><h2>'+esc(title)+'</h2><button type="button" class="g-icon-button" aria-label="Close dialog">×</button></div>'+body;dialog.querySelector('button').onclick=()=>closeDialog(dialog);dialog.addEventListener('cancel',event=>{event.preventDefault();closeDialog(dialog);},{once:true});document.body.append(dialog);dialog.showModal();onReady?.(dialog);return dialog;}
  let audio=null,ambient=null;
  function sound(settings,type='confirm'){if(!settings.effects)return;try{audio=audio||new(window.AudioContext||window.webkitAudioContext)();audio.resume();const oscillator=audio.createOscillator(),gain=audio.createGain();oscillator.type='sine';oscillator.frequency.value=type==='correct'?660:440;gain.gain.setValueAtTime(.035,audio.currentTime);gain.gain.exponentialRampToValueAtTime(.001,audio.currentTime+.18);oscillator.connect(gain).connect(audio.destination);oscillator.start();oscillator.stop(audio.currentTime+.2);}catch{}}
  function ambience(settings){if(ambient){ambient.stop();ambient=null;}if(!settings.ambience)return;try{audio=audio||new(window.AudioContext||window.webkitAudioContext)();audio.resume();const oscillator=audio.createOscillator(),gain=audio.createGain();oscillator.frequency.value=110;oscillator.type='sine';gain.gain.value=.008;oscillator.connect(gain).connect(audio.destination);oscillator.start();ambient=oscillator;}catch{}}
